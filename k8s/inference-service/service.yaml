apiVersion: v1
kind: Service
metadata:
  name: inference-service
  namespace: capstone
  labels:
    app: inference-service
spec:
  selector:
    app: inference-service
  ports:
    - name: http
      port: 8001
      targetPort: 8001
  type: ClusterIP
