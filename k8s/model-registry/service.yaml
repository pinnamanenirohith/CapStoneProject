apiVersion: v1
kind: Service
metadata:
  name: model-registry
  namespace: capstone
  labels:
    app: model-registry
spec:
  selector:
    app: model-registry
  ports:
    - name: http
      port: 8002
      targetPort: 8002
  type: ClusterIP
